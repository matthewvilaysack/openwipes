# OpenWipes

A community map of every public-usable bathroom in San Francisco, rated by people who have actually been.

- `prototype/` is the live static prototype (Leaflet + CARTO tiles).
- `scripts/fetch_seed.py` refreshes `prototype/seed.js` from DataSF Pit Stops and OpenStreetMap.
- `docs/PRD.md` is the product spec.
